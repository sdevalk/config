{
    "port": "$env.MY_ENV_PORT",
    "host": "$env.MY_ENV_HOST",
    "options": {
        "username": "$env.MY_ENV_USERNAME",
        "keys": {
            "password": "$env.MY_ENV_PASSWORD",
            "salt": "no$env.MY_ENV_SALT"
        }
    }
}

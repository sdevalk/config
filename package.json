{
  "name": "config",
  "version": "3.0.1",
  "description": "config",
  "engines": {
    "node": ">=8.9.0"
  },
  "private": true,
  "license": "BSD-3-Clause",
  "main": "lib/index.js",
  "repository": "git://github.com/sdevalk/config.git",
  "scripts": {
    "lint": "lab -d -f -L",
    "test": "lab -L -a @hapi/code -t 100",
    "test-cov-html": "lab -a @hapi/code -r html -o coverage.html"
  },
  "dependencies": {
    "confidence": "4.0.2",
    "fs-extra": "8.0.1"
  },
  "devDependencies": {
    "@hapi/code": "5.3.1",
    "@hapi/lab": "18.1.2"
  }
}
